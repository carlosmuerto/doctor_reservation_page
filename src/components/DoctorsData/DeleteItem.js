import NavBar from '../NavBar/Navbar';

const DeleteItem = () => (
  <section className="margin_top">
    <NavBar name="Delete Item" />
    <h1>Delete page</h1>
    <h4>Here, we will display a list with all the elements what we can delete</h4>
  </section>
);

export default DeleteItem;
