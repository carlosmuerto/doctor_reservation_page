import NavBar from '../NavBar/Navbar';

const AddItem = () => (
  <section className="margin_top">
    <NavBar name="Add Item" />
    <h1>Add a new Doctor:</h1>
  </section>
);

export default AddItem;
